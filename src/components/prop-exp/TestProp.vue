<script setup>
defineProps(["reactiveObj", "refSimple", "refObj"]);
/*const reactiveObj = props.reactiveObj;
const refSimple = props.refSimple;
const refObj = props.refObj;

console.log({ reactiveObj, isRef: isRef(reactiveObj) });
console.log({ refSimple, isRef: isRef(refSimple) });
console.log({ refObj, isRef: isRef(refObj) });*/
</script>
<template>
  <div>Hi</div>
</template>
